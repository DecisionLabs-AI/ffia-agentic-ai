# =============================================================================
# FFIA Project Dependencies — W2
# Install with: pip install -r requirements.txt
# =============================================================================

# --- UI Framework ---
streamlit>=1.45.0

# --- LLM + Agent Framework (W2: LangChain + LangGraph + Gemini via Vertex AI) ---
langchain>=0.3.0
langchain-google-vertexai>=2.0.0
langchain-google-genai>=2.0.0
langchain-community>=0.3.0
langgraph>=0.2.0

# --- Google Cloud (BigQuery + Vertex AI auth via ADC) ---
google-cloud-bigquery>=3.11.0
google-auth>=2.28.0
pyarrow>=14.0.0

# --- Web Search (DuckDuckGo, no API key required) ---
ddgs>=9.0.0

# --- Environment Variables ---
python-dotenv>=1.0.0

# --- Data Processing ---
pandas>=2.0.0
requests>=2.31.0
